For Dialogs on MacOS:

> brew cleanup python-tk@3.11

Ubuntu:

> sudo apt install python3-tk

---

Needed image libraries:

> pip install opencv-python
> pip install pillow
> pip install scikit-image
> pip install ImageHash
